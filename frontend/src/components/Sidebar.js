import React from 'react';
import { Box, Typography } from '@mui/material';

const Sidebar = () => (
  <Box p={2}>
    <Typography variant="h6">Sidebar (TODO)</Typography>
  </Box>
);

export default Sidebar;
